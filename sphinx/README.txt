Sphinx is used to generate the documentation. You can build it yourself
(if you have Sphinx installed) by double-clicking build.bat or by locating
the 'sphinx' directory and using this in command-line:

$ sphinx-build . ../doc